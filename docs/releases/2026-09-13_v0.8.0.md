<!-- markdownlint-disable MD013 -->

# Release Evidence Pack — v0.8.0

**Data:** 2026-09-13
**Tipo:** feature (minor) — estável publicada após validação da candidata `v0.8.0-rc.1`
**Versão anterior estável:** `v0.7.0` (em produção desde 2026-09-08)
**Repositório:** `carlosapgomes/ats-web`
**Tag/commit:** `v0.8.0` / commit deste pack (mesmo conteúdo da rc; o commit difere apenas por este pack)
**GitHub Release:** estável (move `latest` e `0.8`)

## Resumo executivo

**v0.8.0 = Ecoendoscopia e CPRE como procedimentos independentes.** Detalhes
completos, rastreabilidade slice a slice e specs no
[evidence pack da candidata](./2026-09-13_v0.8.0-rc.1.md) — o conteúdo de
código é idêntico (`v0.8.0-rc.1` validada sem correções intermediárias).

Em resumo: catálogo/matriz fechados de cinco conjuntos, writer LLM strict 3.0
(análise única por caso), hard rule determinística de imagem abdominal
fail-closed, decisão médica `trocar e aprovar` transacional sem reanálise,
jornadas de médico/CHD/NIR/gestor por procedimento (declarado/detectado/
autorizado), follow-up do Supervisor restrito a procedimentos autorizados,
runbook de deploy + precheck de downgrade + flags de intake independentes
web-only (`false` por default — ativação sequencial Eco → CPRE é ato
operacional humano).

## Validação da candidata v0.8.0-rc.1

- Workflow `release-image.yml` verde; imagem publicada com digest único
  `sha256:8f4f6cfd…` em ambas as formas da tag; aliases estáveis preservados
  (`latest` inalterado; `0.8` ausente durante a prerelease).
- Boot da imagem validado contra banco de testes isolado: `manage.py check`
  sem issues; `showmigrations cases` confirma `0019` no binário;
  `check_specialized_procedure_downgrade` executa e retorna JSON estável com
  `status: allowed` (exit 0).
- Quality gate do commit-base (926aac6): ruff ✓ · format ✓ · mypy 289
  arquivos ✓ · pytest **3709 passed** ✓ · `openspec validate --strict` ✓.

## Qualidade

Ver pack da rc. Uma única migration (`cases.0019`, AlterField de choices).
ADR-0006/ADR-0007, runbook dedicado, manual do usuário e PROJECT_CONTEXT
atualizados.

## Plano de rollout

Cutover CRÍTICO conforme `docs/deploy/support-independent-echoendoscopy-cpre-workflows.md`:
backup fail-closed validado por conteúdo → preflight (flags false, drenagem,
precheck `allowed` com a imagem nova) → janela (parar writers → migrate 0019
→ seed prompts 3.0 → verificações binárias) → subir com flags false → smoke
de regressão EDA/Colonoscopia → evidência da fronteira de rollback (precheck
`blocked`) → **piloto de Ecoendoscopia com aceite humano** → CPRE somente
depois. A imagem de produção é a estável desta release; rollback suportado
pós-primeiro-write-3.0 é flags off + schema 3.0 + fix-forward.
