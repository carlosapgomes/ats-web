<!-- markdownlint-disable MD013 -->

# Release Evidence Pack — v0.5.2

**Data da release:** 2026-08-31
**Versão anterior:** `v0.5.1`
**Tipo:** correção de erro (patch) — publicação estável direta, sem candidata rc

**Tag:** `v0.5.2`
**Commit:** commit de preparação da release (descendente de `09d3dff`)
**GitHub Release:** <https://github.com/carlosapgomes/ats-web/releases/tag/v0.5.2>

## Resumo executivo

A `v0.5.2` corrige uma inconsistência entre o contexto enviado ao LLM2 e o
conjunto de procedimentos validado pelo pipeline. Quando o LLM1 extraía EDA e
Colonoscopia, mas a reconciliação determinística mantinha somente um desses
procedimentos como solicitação atual, o prompt do LLM2 ainda continha os dois.
O modelo podia responder com duas recomendações, enquanto o validator exigia
corretamente apenas o conjunto reconciliado, levando o caso a `FAILED` por
`procedure set mismatch`.

O LLM2 agora recebe uma cópia efêmera do artefato LLM1 com
`requested_procedures` limitado ao conjunto reconciliado e uma lista JSON
fechada no prompt. O artefato original persistido em `Case.structured_data`
permanece intacto para auditoria. A igualdade exata continua fail-closed; apenas
um mismatch schema-válido pode consumir um retry corretivo tipado e one-shot.

## Correção do erro

| Antes | Depois |
| --- | --- |
| Contexto bruto do LLM1 podia mostrar procedimento descartado pela reconciliação | Visão efêmera contém somente itens originais do conjunto reconciliado |
| Prompt não declarava de forma fechada o conjunto downstream | Prompt inclui lista JSON canônica e exige exatamente um item por procedimento |
| Mismatch falhava sem tentativa corretiva específica | Erro tipado permite exatamente um retry de conjunto |
| Controle de retry dependia apenas do guard pt-BR existente | Budgets de conjunto e idioma são independentes, one-shot e limitados a três chamadas físicas no total |

## Change incluído

| Change | Entrega |
| --- | --- |
| [`fix-llm2-reconciled-procedure-set`](../../openspec/archive/fix-llm2-reconciled-procedure-set/) | Projeção canônica efêmera no boundary do orchestrator, lista fechada no prompt, mismatch tipado, retry finito e cobertura integrada EDA-only/Colonoscopia-only/combinado. |

Rastreabilidade: planejamento `dd1dfac` → implementação `a32963c` → correção
documental `09d3dff` → archive com promoção da spec no commit da release.

## Spec modificada

- [`procedure-neutral-analysis`](../../openspec/specs/procedure-neutral-analysis/spec.md)
  — o requisito *Policy e recomendação são por procedimento* agora define:
  - uma análise LLM2 conjunta por caso, sem dividir a chamada por procedimento;
  - visão efêmera restrita ao conjunto reconciliado e lista JSON fechada;
  - preservação do artefato original do LLM1;
  - igualdade exata sem omissão, duplicata ou adição;
  - um retry corretivo exclusivo para mismatch schema-válido;
  - falha explícita e ausência de recomendação parcial se o mismatch persistir;
  - erros de JSON/schema/IDs/duplicata sem consumo do retry de conjunto.

## Contratos preservados

- `Case.structured_data` e `result1.structured_data` não são mutados;
- policy, prior-case context e validação continuam governados pelo conjunto
  reconciliado;
- detector, regex e aliases não foram ampliados;
- schemas Pydantic, prompts versionados, policy, FSM, models, filas e permissões
  permanecem inalterados;
- não há migration nem transformação de dados;
- nenhum retry do job django-q ou recuperação automática de casos `FAILED` foi
  criado;
- um segundo mismatch permanece fail-closed e não persiste `suggested_action`
  parcial.

## Validação

Executada na preparação da release, após archive e promoção da spec:

```text
uv run ruff check .                     → All checks passed
uv run ruff format --check .            → 232 files already formatted
uv run mypy .                           → Success: no issues found in 257 source files
uv run pytest                           → 3184 passed, 0 failed, 0 errors (exit 0)
uv run openspec validate --all --strict → 8 specs válidas, 0 falhas
```

Evidência TDD e verificação independente:

- RED reproduzido no `BASE_REF` `dd1dfac`: 8 falhas semânticas esperadas e 1
  teste de caracterização passando;
- testes-alvo após a correção: 77 passed;
- suíte completa: 3184 passed, superior à baseline da `v0.5.1` (3174), com
  zero failures/errors;
- escopo funcional limitado a quatro arquivos e sem alteração de migrations,
  FSM, schemas, detector, policy, filas ou prompts versionados.

## Imagem OCI

A publicação da GitHub Release dispara o workflow `release-image.yml`. Digest,
aliases e URL do run serão registrados após a conclusão do workflow.

```text
ghcr.io/carlosapgomes/ats-web:v0.5.2
PENDENTE — aguardando release.published
```

## Rollout operacional pendente

A publicação disponibiliza o artefato, mas **não executa deploy**, migration ou
reinício dos serviços de produção. Após deploy explícito da `v0.5.2`:

1. executar smoke EDA-only, Colonoscopia-only e combinado até `WAIT_DOCTOR`;
2. confirmar o conjunto correto no evento `LLM2_OK` e ausência de novo
   `procedure set mismatch`;
3. reapresentar uma única vez as três ocorrências afetadas registradas no
   change;
4. preservar sem edição/reabertura todas as tentativas antigas em `FAILED`.

## Rollback

- **Gatilho:** regressão mais grave comprovada no pipeline após o deploy.
- **Ação:** redeploy de
  `ghcr.io/carlosapgomes/ats-web:v0.5.1@sha256:591d5a7a306eb6f4b3f4b99343c320f50b93f0933252f410346cc5064bbf271b`.
- **Dados:** nenhuma reversão de banco é necessária; o hotfix não contém
  migration nem rewrite de artefatos.
- **Observação:** o rollback restaura a inconsistência de contexto corrigida e
  só deve ser usado diante de regressão mais severa.

## Checklist de publicação

- [x] Change arquivado e spec promovida.
- [x] Quality gate completo verde.
- [x] RED/GREEN e invariantes de auditoria verificados independentemente.
- [ ] GitHub Release publicada.
- [ ] Workflow `release-image.yml` concluído com sucesso.
- [ ] Tags `v0.5.2`, `0.5.2`, `0.5` e `latest` verificadas no GHCR.
- [ ] Deploy e smoke em produção (operação separada desta publicação).
