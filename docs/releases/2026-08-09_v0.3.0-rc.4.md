# Release Evidence Pack — v0.3.0-rc.4

**Data:** 2026-08-09
**Tipo:** prerelease candidata
**Versão anterior:** `v0.3.0-rc.3`
**Repositório:** `carlosapgomes/ats-web`
**Tag/commit:** tag planejada sobre o commit de fechamento desta candidata
**GitHub Release:** <https://github.com/carlosapgomes/ats-web/releases/tag/v0.3.0-rc.4>

## Resumo executivo

Consolida a sequência de hotfixes do fluxo combinado e um bugfix de UX. A
`v0.3.0-rc.3` comprovou em produção o contrato LLM neutro 2.0: a API aceitou o
strict schema com união `anyOf` e um caso real de teste chegou a
`WAIT_DOCTOR`. Esta candidata adiciona o ajuste de UX na tela de decisão
médica: após erro de validação no formulário, a página agora rola até o
primeiro campo inválido em vez de voltar ao topo.

## Changes incluídos desde v0.3.0-rc.3

| Change | Entrega resumida |
|---|---|
| [`fix-doctor-decision-scroll-to-form-errors`](../../openspec/archive/fix-doctor-decision-scroll-to-form-errors/) | Marcador SSR `data-scroll-to-errors` + `decision.js` rolando ao primeiro `.is-invalid`; QUICK, sem migration. |

## Evidência de produção (v0.3.0-rc.3)

- Caso real `b5ca4434-b39b-484d-87df-c7d8fb05e209` (ocorrência 4998108)
  processado até `WAIT_DOCTOR` com o schema strict V2 contendo união `anyOf`.
- Confirma a resolução dos incidentes de `v0.3.0-rc.1`/`v0.3.0-rc.2`.

## Specs atualizadas

- [`per-procedure-medical-decision`](../../openspec/specs/per-procedure-medical-decision/spec.md)
  — novo requisito: re-renderização com erro de validação marca o formulário
  para rolagem até o primeiro campo inválido.

## Evidência de validação

Quality gate executado no commit do bugfix e novamente na preparação da
release:

```text
uv run ruff check .                 → All checks passed
uv run ruff format --check .        → 232 files already formatted
uv run mypy .                       → Success: no issues found in 257 source files
uv run pytest                       → 3139 passed, 0 failed, 0 errors
uv run openspec validate --all --strict → 7 specs válidas, 0 falhas
```

RED reproduzido contra o BASE_REF: teste de marcador falha antes do ajuste.

## Rollout e rollback

- Sem migration: estado de banco idêntico antes/depois.
- Servidor temporário: `git pull --ff-only` + `docker compose build` +
  `up -d` (web/worker/pdf_worker).
- Rollback: revert do commit + rebuild.

## Evidência de publicação

A completar após `release.published`:

- **Commit da tag:** pendente
- **Workflow:** pendente
- **Conclusão:** pendente
- **Digest GHCR:** pendente
- **Aliases estáveis (`latest`, `0.2`) preservados:** pendente

## Checklist da candidata

- [x] Change arquivado e spec promovida.
- [x] Quality gate completo verde.
- [x] Smoke da candidata anterior comprovado em produção.
- [ ] GitHub Release publicada.
- [ ] Workflow `release-image.yml` concluído com sucesso.
- [ ] Imagens `v0.3.0-rc.4` e `0.3.0-rc.4` verificadas no GHCR.
- [ ] Aliases estáveis comprovadamente inalterados.
- [ ] Confirmação de UX do scroll em produção.
